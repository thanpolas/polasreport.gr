version: '3'
services:
  jekyll:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - JEKYLL_GITHUB_TOKEN=${JEKYLL_GITHUB_TOKEN:-}
      - LISTEN_FORCE_POLLING=1
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - ./:/srv/jekyll
    tty: true
